import { IThemeSwitcher } from './ThemeSwitcher';

const themeSwitcher: IThemeSwitcher = {
  className: 'p-3',
  asToggle: true,
  iconSize: 5
};

export const mockThemeSwitcherProps = {
  themeSwitcher,
};
